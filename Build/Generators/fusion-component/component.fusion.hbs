prototype({{packageKey}}:Component.{{type}}.{{name}}) < prototype(Neos.Fusion:Component) {
    @styleguide {
        title = '{{name}}'
        description = '
            ## Example Usage

            ```afx
            <{{packageKey}}:Component.{{type}}.{{name}}
                />
            ```
        '

        props {

        }
    }

    @propTypes {
        // @TODO: Empty Generated PropTypes
    }

    renderer = afx`
    `
}