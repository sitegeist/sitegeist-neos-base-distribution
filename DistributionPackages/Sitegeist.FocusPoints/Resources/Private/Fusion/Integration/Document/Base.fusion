prototype(Sitegeist.FocusPoints:Document.Base) < prototype(Neos.Fusion:Component) {
    title = ${q(documentNode).property('title')}
    titleOverride = ${q(documentNode).property('titleOverride')}
    body = null

    renderer = Neos.Neos:Page {
        head {
            metaViewport = '<meta name="viewport" content="width=device-width">'
            stylesheets = Sitegeist.FocusPoints:Resource.StyleSheets

            titleTag {
                content = ${props.titleOverride ? props.titleOverride : props.title}
                content.@process.environmentPrefix {
                    expression = ${Configuration.setting('Sitegeist.FocusPoints.environmentPrefix') + ' ' + value}
                    @if.isSet = ${!String.isBlank(Configuration.setting('Sitegeist.FocusPoints.environmentPrefix'))}
                }
            }
        }

        body = ${props.body}

        bodyScripts = Sitegeist.FocusPoints:Resource.BodyScripts
        bodyScripts.@position = 'before closingBodyTag'
    }
}
