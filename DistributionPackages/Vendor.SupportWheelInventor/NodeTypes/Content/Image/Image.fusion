prototype(Vendor.SupportWheelInventor:Content.Image) < prototype(Neos.Neos:ContentComponent) {
    renderer = PackageFactory.AtomicFusion.PresentationObjects:Slot {
        presentationObject = ${SupportWheelInventor.Image.renderContent(node, documentNode, site, Neos.Node.subgraphForNode(documentNode), renderingMode.isEdit)}
    }
}
