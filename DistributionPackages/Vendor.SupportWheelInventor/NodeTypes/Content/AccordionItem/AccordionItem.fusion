prototype(Vendor.SupportWheelInventor:Content.AccordionItem) < prototype(Neos.Neos:ContentComponent) {
    renderer = PackageFactory.AtomicFusion.PresentationObjects:Slot {
        presentationObject = ${SupportWheelInventor.AccordionItem.renderAsContent(node, documentNode, site, Neos.Node.subgraphForNode(documentNode), renderingMode.isEdit)}
    }
}
