prototype(Vendor.SupportWheelInventor:Content.Text) < prototype(Neos.Neos:ContentComponent) {
    renderer = PackageFactory.AtomicFusion.PresentationObjects:Slot {
        presentationObject = ${SupportWheelInventor.Text.renderAsContent(node, documentNode, site, Neos.Node.subgraphForNode(documentNode), renderingMode.isEdit)}
    }
}
